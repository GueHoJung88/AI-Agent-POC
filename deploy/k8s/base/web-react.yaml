apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-react
  namespace: kpmg-poc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-react
  template:
    metadata:
      labels:
        app: web-react
    spec:
      containers:
        - name: web-react
          image: web-react:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: web-react
  namespace: kpmg-poc
spec:
  selector:
    app: web-react
  ports:
    - port: 80
      targetPort: 80
